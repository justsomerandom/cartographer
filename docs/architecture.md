# Architecture

This document will describe Cartographer's UI features, local repository access model, analysis pipeline, parser boundaries, Git integration, and graph data model as implementation begins.
